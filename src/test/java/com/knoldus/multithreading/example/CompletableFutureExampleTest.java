package com.knoldus.multithreading.example;

import java.util.concurrent.ExecutionException;
import org.junit.Test;

public class CompletableFutureExampleTest {
    /**
     * Method under test: {@link CompletableFutureExample#createCompletableFuture()}
     */
    @Test
    public void testCreateCompletableFuture() throws InterruptedException, ExecutionException {
        // TODO: Complete this test.
        //   There are no fields that could be asserted on.

        (new CompletableFutureExample()).createCompletableFuture();
    }
}

